# Supabase Database

This repo keeps one canonical database SQL file:

- `db/supabase_all_in_one.sql`

It contains the full schema, functions, triggers, policies, and consolidated migration history for the app.
