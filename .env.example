# =============================================================================
# EduGuide College Platform - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values.
# NEVER commit .env.local to git (it's in .gitignore).
#
# All these variables must also be set as GitHub repository secrets
# for CI/CD to work. See the workflow files for details.
# =============================================================================

# --- Supabase (Client-side - safe to expose) ---
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# --- Supabase (Server-side - KEEP SECRET) ---
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# --- Vercel (for CI/CD deployments) ---
# Get these from: https://vercel.com/account/tokens
# VERCEL_TOKEN="your-vercel-token"
# VERCEL_ORG_ID="your-org-id"
# VERCEL_PROJECT_ID="your-project-id"
