# =============================================================================
# EduGuide College Platform - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values.
# NEVER commit .env.local to git (it's in .gitignore).
#
# All these variables must also be set as GitHub repository secrets
# for CI/CD to work. See the workflow files for details.
# =============================================================================

# --- Supabase (Client-side - safe to expose) ---
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# --- Supabase (Server-side - KEEP SECRET) ---
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# --- Stripe (Payments) ---
STRIPE_SECRET_KEY="sk_live_or_test_key"
STRIPE_WEBHOOK_SECRET="whsec_webhook_signing_secret"
NEXT_PUBLIC_STRIPE_PRICE_BASIC="price_basic"
NEXT_PUBLIC_STRIPE_PRICE_PREMIUM="price_premium"
NEXT_PUBLIC_STRIPE_PRICE_ELITE="price_elite"

# --- Site config ---
# Use your public site URL in production (for redirects/checkout return URLs)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# --- Email notifications (optional) ---
SENDGRID_API_KEY="sg_api_key"
CONTACT_TO_EMAIL="support@eduguide.online"

# --- Internal admin bootstrap (optional, server-only) ---
ADMIN_BOOTSTRAP_TOKEN="long-random-token"
ADMIN_SEED_EMAIL="admin@eduguide.online"
ADMIN_SEED_PASSWORD="strong-admin-password"

# --- Vercel (for CI/CD deployments) ---
# Get these from: https://vercel.com/account/tokens
# VERCEL_TOKEN="your-vercel-token"
# VERCEL_ORG_ID="your-org-id"
# VERCEL_PROJECT_ID="your-project-id"
